/**
 * Application-level providers for the Ledger bounded context.
 * Add command handlers, query handlers, event handlers, etc. here.
 */

export const LedgerApplicationProviders = [
  // TODO: add command/query/event handlers here
];
