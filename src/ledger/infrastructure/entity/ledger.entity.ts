/**
 * Infrastructure persistence entity for Ledger.
 * Map this to your ORM (TypeORM, Prisma, Sequelize, etc).
 */
export class LedgerEntity {
  id!: string;
  // TODO: add columns/fields matching your persistence model
}
